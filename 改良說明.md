# 衛星姿態控制 PPO 專案改良說明

## 原本專案的問題

1. Reward bonus 有計算但沒有實際回傳。
   - 原本程式中 `r1 = self.r_t(r_inter, phi)` 有算加分後 reward。
   - 但最後使用 `reward = r_inter`，所以 `r1` 沒有被用到。

2. Reward 設計偏單一。
   - 原本主要鼓勵姿態誤差變小。
   - 但 detumbling 任務也應該同時要求角速度下降，並避免控制力矩過大。

3. Done 條件過於嚴格。
   - 原本使用整個狀態向量接近目標狀態作為成功條件。
   - 實務上比較合理的是「姿態誤差小於某角度」且「角速度小於門檻」。

4. 缺少傳統控制器比較。
   - 只有 PPO 結果時，很難說明 AI 是否真的有改善。
   - 加入 PD controller baseline 後，可以比較 PPO 與傳統控制方法的表現。

## 已完成的改良

1. 修正 reward 使用方式。
2. 新 reward 包含：
   - 姿態誤差懲罰
   - 角速度懲罰
   - 控制力矩懲罰
   - 姿態誤差改善量獎勵
   - 達成目標時的 success bonus
3. 新 done 條件：
   - 姿態誤差小於 5 度
   - 角速度 norm 小於 0.01
   - 或超過最大步數 timeout
4. 新增 `baseline_pd.py`。
   - 使用 quaternion error + angular velocity 做 PD control。
5. 新增分析輸出：
   - Euler angle 圖
   - reward curve
   - attitude error 圖
   - angular-rate norm 圖
   - control torque 圖
   - episode history CSV

## 可以怎麼說這份改良

這份改良版將原本的 PPO 衛星姿態控制範例，從單純展示 AI 訓練，提升成可以進行工程比較的模擬實驗。改良後的環境不只要求衛星接近目標姿態，也要求旋轉速率降低，並懲罰過大的控制力矩。除此之外，加入傳統 PD controller 作為 baseline，可以用同一個初始姿態比較 PPO 與傳統控制方法在收斂速度、最終姿態誤差、角速度與控制力矩上的差異。

## 後續可再升級的方向

1. 把 EnduroSat telemetry 的姿態角與角速度轉成初始狀態，讓 `reset()` 從真實衛星資料抽樣。
2. 加入 PPO 與 PD 的自動比較表格。
3. 嘗試 SAC、DDPG 或 TD3 等連續控制演算法。
4. 加入 reaction wheel saturation、magnetorquer、sensor noise 等更接近真實衛星的限制。
